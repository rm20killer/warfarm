export function isOverwolf(): boolean {
  return false;
}

export function safeDragMove(_windowName: string = 'desktop'): void {
  // Pure web environment - no window dragging needed
}

export function safeMinimize(): void {
  // Pure web environment - no-op
}

export function safeClose(): void {
  // Pure web environment - no-op
}

